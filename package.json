{
  "name": "eslint-plugin-uship-omniscient",
  "version": "0.14.0",
  "description": "An eslint rule to lint for, and fix, usages of omniscient.",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Richard Simpson <rsimpson@uship.com>",
  "main": "dist/index.js",
  "scripts": {
    "lint:types": "tsc --noEmit",
    "format": "prettier --write {lib,tests}/**/*.{ts,tsx}",
    "build": "run-s clean lint:** build:**",
    "clean": "rimraf ./dist/",
    "build:typescript": "tsc",
    "test": "jest",
    "test:debug": "jest --inspect-brk",
    "prepublishOnly ": "run-s build"
  },
  "devDependencies": {
    "@types/eslint": "^4.16.5",
    "@types/jest": "^23.3.13",
    "@types/node": "^10.12.12",
    "@types/prettier": "^1.15.2",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.9.0",
    "eslint-plugin-react": "^7.11.1",
    "jest": "23.x",
    "jest-diff": "^24.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.5",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.1"
  },
  "engines": {
    "node": ">=8.x"
  },
  "license": "ISC",
  "peerDependencies": {
    "eslint": "~5.9.0"
  },
  "dependencies": {
    "prettier": "^1.15.3"
  }
}
